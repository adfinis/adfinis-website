#!/bin/sh

if ! command -v node >/dev/null 2>&1; then
  echo "commit-msg hook: node not found, commit message not checked" >&2
  exit 0
fi

exec node .github/scripts/commit-convention.mjs --file "$1"
